<%= simple_form_for @restaurant do |r|%>
  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :category, label: 'Please choose a category', collection: %w[Chinese Italian Japanese French Belgian]%>
  <%= r.submit %>
<% end %>
